{
    "name": "Bookstore Service",
    "image": "mcr.microsoft.com/devcontainers/java:21",
    "features": {
        "ghcr.io/devcontainers/features/postgres:1": {
            "version": "16",
            "password": "book123"
        }
    },
    "postCreateCommand": "sudo service postgresql start && sudo runuser -l postgres -c \"psql -c 'CREATE USER bookuser WITH PASSWORD \\\"book123\\\";'\" && sudo runuser -l postgres -c \"psql -c 'CREATE DATABASE bookstoredb OWNER bookuser;'\""
}
