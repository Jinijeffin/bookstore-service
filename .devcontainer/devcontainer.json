{
    "name": "Bookstore Service",
    "features": {
        "ghcr.io/devcontainers/features/postgres:1": {
            "version": "16",
            "password": "book123"
        }
    },
    "postCreateCommand": "sudo sed -i 's/local   all             postgres                                peer/local   all             postgres                                trust/' /etc/postgresql/16/main/pg_hba.conf && sudo service postgresql restart && sudo runuser -l postgres -c \"psql -c 'CREATE USER bookuser WITH PASSWORD \\\"book123\\\";'\" ; sudo runuser -l postgres -c \"psql -c 'CREATE DATABASE bookstoredb OWNER bookuser;'\""
}
